import * as React from "react"
import * as ReactDOM from "react-dom"
import App from "./App"

// simple comment to re-trigger snyk

const root = document.getElementById("root")
ReactDOM.render(<App />, root)
